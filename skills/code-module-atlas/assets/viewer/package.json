{
  "name": "code-module-atlas-viewer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:meet-data": "node scripts/build-meet-dataset.mjs",
    "prepare:meet": "node scripts/prepare-meet-assets.mjs",
    "dev": "vite --host 127.0.0.1",
    "build": "npm run build:meet-data && npm run prepare:meet && vite build",
    "preview": "vite preview --host 127.0.0.1"
  },
  "devDependencies": {
    "vite": "^6.3.6"
  }
}
